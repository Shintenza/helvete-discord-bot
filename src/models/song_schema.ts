import { ShoukakuTrack } from 'shoukaku';

interface Song {
    title: string;
    author: string;
    duration: number;
    uri: string;
    thumbnail: string;
    requester: string;
    track: string;
}
export default Song;
